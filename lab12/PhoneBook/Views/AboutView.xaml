<UserControl x:Class="PhoneBook.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="О программе" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

        <TextBlock Grid.Row="1" Text="{Binding AppName}" FontSize="16" FontWeight="SemiBold" Margin="0,5" HorizontalAlignment="Center"/>

        <TextBlock Grid.Row="2" Text="{Binding Version}" FontSize="14" Foreground="Gray" Margin="0,5" HorizontalAlignment="Center"/>

        <TextBlock Grid.Row="3" Text="{Binding Author}" FontSize="14" Margin="0,5" HorizontalAlignment="Center"/>

        <TextBlock Grid.Row="4" Text="{Binding Description}" FontSize="12" Foreground="DarkGray" Margin="0,20" HorizontalAlignment="Center" TextWrapping="Wrap"/>
    </Grid>
</UserControl>