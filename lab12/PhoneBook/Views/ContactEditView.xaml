<UserControl x:Class="PhoneBook.Views.ContactEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Редактирование контакта" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1" Text="Имя:" FontWeight="SemiBold" Margin="0,5"/>
        <TextBox Grid.Row="2" Text="{Binding EditName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Padding="5"/>

        <TextBlock Grid.Row="3" Text="Телефон:" FontWeight="SemiBold" Margin="0,5"/>
        <TextBox Grid.Row="4" Text="{Binding EditPhone, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Padding="5"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,20" HorizontalAlignment="Center">
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Width="120" Height="35" Margin="0,0,10,0"/>
            <Button Content="Отмена" Command="{Binding CancelCommand}" Width="120" Height="35"/>
        </StackPanel>
    </Grid>
</UserControl>